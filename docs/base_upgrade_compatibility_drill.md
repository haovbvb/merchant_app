# 基座升级兼容演练记录

## 1. 目标

验证基座升级后，业务应用入口不需要连锁修改，仍可通过原有方式启动。

## 2. 兼容口径

- 既有入口 AppEntryPoint 不改代码仍可运行
- 新增配置能力为可选参数，不破坏旧调用方式
- 升级后静态检查与测试通过

## 3. 本次验证（2026-03-24）

验证对象：

- 既有入口：lib/main.dart
- 基座变更：app_shell 增加 feature flags 与可配置 initialLocation

验证结果：

- 既有入口仍使用 AppShellApp 默认构造调用，未改业务层启动代码
- flutter analyze 通过
- widget 测试通过

结论：本次基座升级未引发业务应用连锁大改。
