name: app_shell
description: App shell package for bootstrap, routing and global lifecycle.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.9.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  feature_auth:
    path: ../feature_auth
  feature_home:
    path: ../feature_home
  feature_profile:
    path: ../feature_profile
  feature_work:
    path: ../feature_work
  design_system:
    path: ../design_system
  flutter_riverpod: ^3.0.3
  go_router: ^14.8.1
  mobile_scanner: ^6.0.11
  foundation:
    path: ../foundation

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
