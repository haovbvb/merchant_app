name: feature_auth
description: Pluggable authentication feature module.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.9.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.0.3
  go_router: ^14.8.1
  networking:
    path: ../networking
  shared_preferences: ^2.5.3
  foundation:
    path: ../foundation

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
