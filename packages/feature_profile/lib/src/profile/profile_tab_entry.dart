import 'package:flutter/widgets.dart';

import 'presentation/profile_tab.dart';

class ProfileTabEntry extends StatelessWidget {
  const ProfileTabEntry({super.key});

  @override
  Widget build(BuildContext context) => const ProfileTab();
}
