name: networking
description: Networking client, interceptors and response models.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.9.0
  flutter: ">=3.24.0"

dependencies:
  dio: ^5.9.0
  foundation:
    path: ../foundation

dev_dependencies:
  flutter_lints: ^5.0.0
  lints: ^5.1.1
  test: ^1.25.8
